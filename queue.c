#include "monty.h"

/**
 * queue_code -  sets the format of the data to a queue
 * @head: pointer to the stack
 * @count: the line number
 * Return: void
 */

void queue_code(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	bus_._flag = 1;
}
