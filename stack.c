#include "monty.h"

/**
 * stack_code - sets the format of the data to a stack 
 * @head: pointer to the stack
 * @count: the line number
 * Return: void
 */

void stack_code(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	bus_._flag = 0;
}
